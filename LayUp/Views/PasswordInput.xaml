<Window x:Class="LayUp.Views.PasswordInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LayUp.Views"
        mc:Ignorable="d"
        DataContext="{Binding PassWordInput, Source={StaticResource Locator}}"
        Title="输入密码" Height="120" Width="300">
        
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel Height="40">
            <Label Content="Password" VerticalAlignment="Center"></Label>
            <Label Content=":" VerticalAlignment="Center"></Label>
            <TextBox Style="{x:Null}" Width="120" VerticalContentAlignment="Center" Text="{Binding PassWord}" InputMethod.InputScope="Number" BorderBrush="Black" Height="30" VerticalAlignment="Center"></TextBox>
            <Label Content="{Binding Hint}"/>
        </DockPanel>
        <DockPanel Grid.Row="1">
            <Button Content="OK" Margin="80,5,5,5" Width="80" Command="{Binding SendPasswordCommand}"></Button>
            <Button Content="Cancel" Margin="5" Width="80" Command="{Binding CancelCommand}" ></Button>
        </DockPanel>
    </Grid>
</Window>
